apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dotnet-app-source-code-persisent-volume-claim
spec:
  volumeName: dotnet-app-source-code-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi